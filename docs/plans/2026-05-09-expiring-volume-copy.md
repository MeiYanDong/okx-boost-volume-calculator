# S15 子 Plan：到期交易量文案收敛

## 阶段目标

把快照预测里的到期提示改成只展示到期交易量，避免用户误解为某个刷量日期本身需要操作。

## 实现边界

1. 计算逻辑不变，仍按快照日和 10 日滚动窗口计算。
2. 前端不展示刷量日期或窗口外日期。
3. 飞书提醒也只展示本次快照对应的到期交易量。

## 当前状态

已完成。预测行和飞书提醒都改为“到期交易量”，不再展示刷量日期。

## 验收标准

1. 快照预测卡片不展示到期交易日。
2. 飞书提醒消息不展示到期交易日。
3. `npm run build` 通过。
