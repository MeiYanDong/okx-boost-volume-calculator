# S8 子 Todo：删除全局状态条

- [x] 删除首页和其他页面共用的 `StatusStrip` 渲染。
- [x] 删除 `StatusStrip` 组件。
- [x] 删除 `.system-strip` 相关 CSS。
- [x] 删除只用于全局状态条的 `progress` 状态。
- [x] 删除只用于全局状态条的 `error` 状态。
- [x] 保留钱包行级别 `progress` 字段。
- [x] 确认代码中不再包含「准备同步钱包归档」。
- [x] 运行 `npm run lint`。
- [x] 运行 `npm run build`。
